// place files you want to import through the `$lib` alias in this folder.
export { default as Hero } from "$lib/components/Hero.svelte"
export { default as About } from "$lib/components/About.svelte"